# 项目整体架构导图

> 本图用于理解 `pe-reverse-analyzer` 的平台分层、数据流转、接口调用与能力状态边界。

## 零、部署架构（单 Go 后端）

```mermaid
flowchart LR
    Browser["React 中文前端"] -->|"HTTP / SSE"| Go["Go Control Plane"]
    Go --> Auth["认证 / RBAC / OAuth / API Token"]
    Go --> Flow["Flow 编排 / 恢复 / 重试 / 人工确认"]
    Go --> Provider["Provider 配置 / 连接测试 / 用量 / 回退"]
    Go --> Storage{"持久化后端"}
    Storage --> PostgreSQL["PostgreSQL"]
    Storage --> JSON["本地 JSON"]
    Go --> Sandbox["Docker / Podman 受限 Worker"]
    Go --> Python["Python 分析引擎 CLI"]
    Python --> Tools["工具 / Skill / Capability Provider"]
    Python --> Evidence["证据 / 报告 / Artifact / 知识"]
    Evidence --> Go
```

部署时只有 `reverse-analyzer-server` 对外监听。Python 不提供 Web API，只作为 Go worker 明确调用的分析引擎；`python -m reverse_analyzer web` 也只负责启动 Go 控制面。

## 一、平台总览

```mermaid
mindmap
  root((pe-reverse-analyzer))
    入口层
      CLI
        analyze
        capability
        source
        protocol
        android
        ios
        engine
        memory
        gui
        patch
        evidence
        environment
        knowledge
        skills
        coverage
        debugger-import
      配置与工作区
        AnalyzerConfig
        runtime dirs
        session workspace
    编排层
      ToolExecutor
        register_builtin_tools
        execute
        ToolResult
      CapabilityRegistry
        register provider
        resolve provider
        list capabilities
      Runtime
        SessionStore
        TraceLogger
        ExperimentStore
    工具层
      静态分析
        file_info
        hash
        strings_extract
        pe_header_scan
        pe_deep_scan
        section_entropy_scan
        packer_detect
        yara_scan
        anti_detection_analyze
      动态分析
        frida_trace
        procmon_trace
        ghidra_decompile
        memory_snapshot
        memory_diff
        memory_address_map
      GUI 与行为
        gui_fingerprint
        gui_resource_extract
        gui_runtime_probe
        gui_visual_parse
        gui_evidence_graph
        gui_state_machine
        gui_behavior_graph
      协议
        protocol_capture
        protocol_infer
        protocol_summarize
        protocol_analyze
      源码重构
        semantic_ir_build
        reconstruction_verify
        reconstruct_project
        reconstruct_gui_project
      移动与引擎
        android_analyze
        ios_analyze
        ipa_analyze
        engine_analyze
      Patch
        binary_patch_apply
        binary_patch_rollback
        validate_patch_plan
        android_elf_patch_plan
        dll_proxy_generate
    能力 Provider 层
      统一生命周期
        CapabilityRequest
        CapabilityPlan
        CapabilityValidation
        CapabilityExecutionResult
        CapabilityRollbackResult
        CapabilityArtifactBundle
      Provider 域
        Android
          android instrumentation
          android native patch
          android rebuild
        iOS
          ios instrumentation
          ios rebuild
        Memory
          memory runtime
          injector
          native hook
          native debugger
          kernel memory
          DMA memory
        Engine
          Unity
          Unreal
          IL2CPP
          Mono
        GUI Graphics
          Windows UIA
          VLM
          render overlay
          graphics runtime
          ImGui renderer
        Protocol
          capture
          infer
          mutate
          replay
        Patch
          PE patch
          DLL proxy
          Android ELF patch
        Security Research
          anti tamper lab
          LLM jailbreak research
          hardware identity
          target control simulator
    证据与输出层
      Evidence Manifest
        hash-backed evidence
        artifact identity
        verification command
      ReportBuilder
        report_json
        report_md
        tool_trace
        capability_audit
        platform_core
        semantic_ir
      Dashboard
        offline command deck
        dashboard_trace
        experiments
      Source Project
        editable source
        build files
        validation metadata
    知识与路由层
      KnowledgeBase
        sample records
        typed documents
        capability outcomes
        strategy stats
        knowledge_context
      SkillCatalog
        discover SKILL.md
        audit metadata
        route skills
        execution boundary
      Coverage
        skill_parity_matrix
        done
        partial
        dependency-gated
        missing
      Environment Validation
        dependency probes
        acceptance fixtures
        retained live evidence
```

## 二、数据流转导图

```mermaid
flowchart TB
    Target["目标输入<br/>EXE/DLL/APK/IPA/PCAP/trace/dump/screenshot/session"] --> GoAPI["Go API / Flow 工作台"]
    GoAPI --> CLI["受控 Python CLI 分析引擎"]

    CLI --> Workspace["配置 / 工作区 / Session"]
    Workspace --> ToolPlan["工具调用计划"]
    Workspace --> CapabilityPlan["能力调用计划"]

    ToolPlan --> ToolExecutor["ToolExecutor.execute()"]
    ToolExecutor --> ToolResult["ToolResult<br/>status/data/error/metadata"]

    CapabilityPlan --> Registry["CapabilityRegistry.resolve()"]
    Registry --> Provider["CapabilityProvider"]
    Provider --> Plan["plan"]
    Plan --> Validate["validate"]
    Validate --> Execute["execute"]
    Execute --> Rollback["rollback 可选"]
    Execute --> Collect["collect_artifacts"]
    Collect --> CapabilityResult["CapabilityExecutionResult"]

    ToolResult --> Evidence["Evidence / Artifacts"]
    CapabilityResult --> Evidence

    Evidence --> Manifest["Evidence Manifest<br/>哈希、路径、来源、验证命令"]
    Evidence --> Report["ReportBuilder<br/>report.json / report.md"]
    Evidence --> Dashboard["Dashboard<br/>离线命令面板"]
    Evidence --> Source["源码重构项目<br/>可编辑/可编译/可验证"]

    Report --> Knowledge["KnowledgeBase"]
    CapabilityResult --> Knowledge
    Knowledge --> Recall["knowledge_context 召回"]
    Recall --> CLI

    Skills["SkillCatalog<br/>SKILL.md 指令资产"] --> Route["能力路由建议"]
    Route --> CLI

    Coverage["Coverage Audit<br/>done/partial/dependency-gated"] --> CLI
    Environment["Environment Validation<br/>外部工具与真实验收"] --> CLI
```

## 三、接口与工具调用导图

```mermaid
flowchart LR
    CLI["CLI 子命令"] --> Decision{"调用路径"}

    Decision -->|"普通本地工具"| Executor["ToolExecutor"]
    Executor --> Register["register_builtin_tools()"]
    Register --> Static["静态工具<br/>PE/YARA/hash/strings/packer"]
    Register --> Dynamic["动态工具<br/>Frida/Procmon/Ghidra/memory"]
    Register --> GUI["GUI 工具<br/>fingerprint/visual/state/evidence"]
    Register --> Protocol["协议工具<br/>capture/infer/summarize/analyze"]
    Register --> Reconstruct["重构工具<br/>semantic_ir/verify/reconstruct"]
    Register --> MobileEngine["移动与引擎<br/>android/ios/engine"]
    Register --> Patch["Patch 工具<br/>binary/ELF/DLL proxy"]

    Static --> Result["ToolResult"]
    Dynamic --> Result
    GUI --> Result
    Protocol --> Result
    Reconstruct --> Result
    MobileEngine --> Result
    Patch --> Result

    Decision -->|"平台能力"| Registry["CapabilityRegistry"]
    Registry --> Provider["Provider"]
    Provider --> LifeCycle["plan -> validate -> execute -> rollback -> collect_artifacts"]
    LifeCycle --> CapResult["CapabilityExecutionResult"]

    Result --> Output["Report / Evidence / Dashboard / Knowledge"]
    CapResult --> Output
```

## 四、状态边界导图

```mermaid
flowchart TB
    Registered["已注册<br/>CLI/ToolExecutor/CapabilityRegistry 能找到入口"] --> Implemented["已实现<br/>有生产代码与非 mock 回归测试"]
    Implemented --> Accepted["完整可用<br/>外部依赖满足并通过真实目标端到端验收"]

    Registered -.不等于.-> Implemented
    Implemented -.不等于.-> Accepted

    Coverage["当前覆盖审计"] --> Done["done: 19"]
    Coverage --> Partial["partial: 9"]
    Coverage --> Gated["dependency-gated: 18"]
    Coverage --> Missing["missing: 0"]

    Partial --> Reason1["能力已有骨架或 bounded subset<br/>但缺完整行为等价/跨目标验收"]
    Gated --> Reason2["依赖真实工具、设备、驱动、图形目标、模型端点或保留验收记录"]
    Done --> Reason3["满足生产代码、测试与既定验收标准"]
```

## 五、模块定位速查

| 模块 | 角色 |
| --- | --- |
| `cmd/reverse-analyzer-server/` | 唯一 Web 后端、认证、Flow、SSE、Provider、持久化与 worker 调度 |
| `frontend/` | React 中文控制台，通过 Go API 工作 |
| `reverse_analyzer/cli.py` | 平台主入口与命令编排 |
| `reverse_analyzer/tools/executor.py` | 本地工具注册与统一执行 |
| `reverse_analyzer/tools/static_tools.py` | 内置工具注册清单 |
| `reverse_analyzer/core/capabilities/registry.py` | 能力 Provider 注册与解析 |
| `reverse_analyzer/core/capabilities/models.py` | Provider 生命周期数据模型 |
| `reverse_analyzer/providers/` | 各领域生产 Provider |
| `reverse_analyzer/runtime/` | session、trace、experiment 持久化 |
| `reverse_analyzer/report/builder.py` | 报告构建与平台核心汇总 |
| `reverse_analyzer/evidence/` | 证据 manifest 与哈希校验 |
| `reverse_analyzer/knowledge/base.py` | 知识库、typed documents、策略经验 |
| `reverse_analyzer/skills/catalog.py` | SKILL.md 发现、审计与路由 |
| `reverse_analyzer/coverage.py` | 能力覆盖矩阵审计 |
| `reverse_analyzer/instructions/` | 跨平台破甲指令部署：四平台适配器 + 统一安全模型 + `reverse-instruct` CLI |
| `docs/skill_parity_matrix.md` | 能力状态事实来源 |
| `docs/acceptance/` | 真实环境验收记录与说明 |

## 六、阅读顺序建议

1. 先看“平台总览”，理解项目被分成入口、编排、工具、Provider、证据、知识六层。
2. 再看“数据流转导图”，理解一次分析从输入到报告、知识召回如何闭环。
3. 然后看“接口与工具调用导图”，区分普通工具调用和 Provider 生命周期调用。
4. 接着看“状态边界导图”，避免把“已接入入口”误判为“真实目标完整可用”。
5. 跨平台指令部署是平台**之外**的独立交付物（`reverse_analyzer/instructions/`，`reverse-instruct` CLI），它注入 AI 客户端而非分析目标，阅读时与主分析引擎分开看；先读 `docs/跨平台破甲指令部署指南.md` 理解四平台注入机制，再对照源码走查。
